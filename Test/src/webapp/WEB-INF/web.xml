<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- Package à scanner -->
    <context-param>
        <param-name>controller-packages</param-name>
        <param-value>com.example.backoffice.controller</param-value>
    </context-param>
    
    <context-param>
        <param-name>user-session-name</param-name>
        <param-value>token</param-value>
    </context-param>

    <!-- Initialisation du framework -->
    <listener>
        <listener-class>com.example.framework.core.ContextInitializer</listener-class>
    </listener>

    <!-- Servlet principale -->
    <servlet>
        <servlet-name>Dispatcher</servlet-name>
        <servlet-class>com.example.framework.servlet.DispatcherServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>Dispatcher</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>
</web-app>
