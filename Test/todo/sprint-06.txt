groupeReservations
    lister les reservations
    trier par date_arrivee croissant
    prendre TA
    trouver les intervalles
    grouper selon les intervalles

assignation
    lister les groupes de reservation
    traiter chaque groupe


traiterGroupe
    trier chaque groupe par nombre_passager desc
    liste des assignees 
    traiter les reservations
    preparer trajet

traiterReservation
    si pas encore assigner
        trouver le meilleur vehicule disponible
        si exist vehicule
            creer un trajet (save trajet)
            remplir vehicule
        sinon
            envoyer la reservation au groupe suivant

trouver vehicule
    lister vehicules disponibles plus proche et suffisante pour la reservation
    prendre min capacite
    si capacite mitovy prendre min trajet
    si meme nbr trajet prendre diesel sinon random

remplirVehicule
    lister groupe
    si pas encore assigner 
    si capaciteRestante >= nombre_passager
        assigner la reservation au vehicule (save trajet_reservation)
        capaciteRestante -= nombre_passager
        mettre dans la liste des assignees

preparerTrajet
    trouver l'heure de depart
    calculer l'itineraire
    save trajet

trouverHeureDepart
    lister la liste assignees suivant la date_arrivee DESC
    heure_arrivee de la première reservation
    trouver heure_retour vehicule
    si heure_retour non null et heure_retour >= heure_arrivee
    retourner heure_retour
    sinon
    retourner heure_arrivee

calculerItineraire
    calculer la distance
    prendre la VM
    calculer duree
    calculer heure_retour

calculerDistance
    lister les reservations du trajet
    prendre l'Aeroport
    distance
    lieu actuel sera l'aeroport
    liste des mbola ts nandalovana
    faire toutes les combinaison possible avec les mbola tsy nandalovana
    trier les combinaison par distance asc puis par ordre alphabetique
    prendre l'hotel plus proche du lieu actuel
    distance +=  distance entre hotel et lieu actuel
    nouveau lieu actuel sera cet hotel
    asorina tao amin'ny liste mbola ts nandalovana
    jusqu’à ce que tsisy inoninona tsony mbola ts nadalovana


List<List<Reservation>> getGroupesReservations(LocalDate date)
void traiterGroupeReservation(LocalDate date, List<Reservation> groupe)
Trajet traiterReservation(Reservation reservation, 
                            List<Reservation> assignees, 
                            List<Reservation> groupeReservations)
Vehicule getVehiculeDisponible(Reservation reservation)
List<TrajetReservation> remplirVehicule(List<Reservation> groupeReservations, 
                                        List<Reservation> assignees, 
                                        Trajet trajet)
void preparerTrajet(List<Reservation> assignees, Trajet trajet)
void calculerItineraire(List<Reservation> assignees, Trajet trajet)
LocalTime getHeureDepart(List<Reservation> assignees)
Double calculerDistance(List<Reservation> assignees)
void assignation(LocalDate date)